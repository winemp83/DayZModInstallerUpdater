<UserControl x:Class="DayZToolIniFile.Views.IniView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DayZToolIniFile.Views"
             xmlns:viewModel = "clr-namespace:DayZToolIniFile.ViewModel" 
             xmlns:vml = "clr-namespace:DayZToolIniFile.VML" 
             xmlns:data ="clr-namespace:XmlIni;assembly=XmlIni"
             vml:ViewModelLocator.AutoHookedUpViewModel="True" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType = "{x:Type data:IniModel}">

            <StackPanel Orientation = "Horizontal">
                <TextBox Text = "{Binding Path = Key, Mode = TwoWay}" 
               Width = "100" Margin = "3 5 3 5"/>

                <TextBox Text = "{Binding Path = Value, Mode = TwoWay}" 
               Width = "100" Margin = "0 5 3 5"/>

            </StackPanel>

        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ListBox ItemsSource = "{Binding Configs}"/>
    </Grid>

</UserControl>
